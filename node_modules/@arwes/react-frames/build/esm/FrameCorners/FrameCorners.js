import React, { useMemo } from 'react';
import { cx } from '@arwes/tools';
import { memo } from '@arwes/react-tools';
import { createFrameCornersSettings } from '@arwes/frames';
import { FrameBase } from '../FrameBase/index.js';
const FrameCorners = memo((props) => {
    const { styled, animated, padding, strokeWidth, cornerLength } = props;
    const settings = useMemo(() => createFrameCornersSettings({ styled, animated, padding, strokeWidth, cornerLength }), [styled, animated, padding, strokeWidth, cornerLength]);
    return (React.createElement(FrameBase, { ...props, className: cx('arwes-frames-framecorners', props.className), settings: settings }));
});
export { FrameCorners };
