import React, { useMemo } from 'react';
import { cx } from '@arwes/tools';
import { memo } from '@arwes/react-tools';
import { createFrameHeaderSettings } from '@arwes/frames';
import { FrameBase } from '../FrameBase/index.js';
const FrameHeader = memo((props) => {
    const { styled, animated, padding, strokeWidth, decoWidth, direction, align, contentLength } = props;
    const settings = useMemo(() => createFrameHeaderSettings({
        styled,
        animated,
        padding,
        strokeWidth,
        decoWidth,
        direction,
        align,
        contentLength
    }), [styled, animated, padding, strokeWidth, decoWidth, direction, align, contentLength]);
    return (React.createElement(FrameBase, { ...props, className: cx('arwes-frames-frameheader', props.className), settings: settings }));
});
export { FrameHeader };
