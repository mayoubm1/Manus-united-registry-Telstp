import React, { useMemo } from 'react';
import { cx } from '@arwes/tools';
import { memo } from '@arwes/react-tools';
import { createFrameNeroSettings } from '@arwes/frames';
import { FrameBase } from '../FrameBase/index.js';
const FrameNero = memo((props) => {
    const { styled, animated, padding, cornerLength, cornerWidth } = props;
    const settings = useMemo(() => createFrameNeroSettings({ styled, animated, padding, cornerLength, cornerWidth }), [styled, animated, padding, cornerLength, cornerWidth]);
    return (React.createElement(FrameBase, { ...props, className: cx('arwes-frames-framenero', props.className), settings: settings }));
});
export { FrameNero };
