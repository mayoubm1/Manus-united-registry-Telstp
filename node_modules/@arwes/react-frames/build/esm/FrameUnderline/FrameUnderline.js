import React, { useMemo } from 'react';
import { cx } from '@arwes/tools';
import { memo } from '@arwes/react-tools';
import { createFrameUnderlineSettings } from '@arwes/frames';
import { FrameBase } from '../FrameBase/index.js';
const FrameUnderline = memo((props) => {
    const { styled, squareSize, strokeWidth, padding } = props;
    const settings = useMemo(() => createFrameUnderlineSettings({ styled, squareSize, strokeWidth, padding }), [styled, squareSize, strokeWidth, padding]);
    return (React.createElement(FrameBase, { ...props, className: cx('arwes-frames-frameunderline', props.className), settings: settings }));
});
export { FrameUnderline };
