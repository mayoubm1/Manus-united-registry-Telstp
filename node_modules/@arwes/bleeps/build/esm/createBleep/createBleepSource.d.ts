type BleepSource = {
    started: boolean;
    stopped: boolean;
    start: () => void;
    stop: () => void;
};
type BleepSourceProps = {
    context: AudioContext;
    buffer: AudioBuffer;
    gain: GainNode;
    loop?: boolean;
    onStop?: () => void;
};
declare const createBleepSource: (props: BleepSourceProps) => BleepSource;
export type { BleepSource, BleepSourceProps };
export { createBleepSource };
