import React, { useEffect, useState } from 'react';
const NoSSR = (props) => {
    const [available, setAvailable] = useState(false);
    useEffect(() => {
        setAvailable(true);
    }, []);
    return React.createElement(React.Fragment, null, available ? props.children : null);
};
export { NoSSR };
