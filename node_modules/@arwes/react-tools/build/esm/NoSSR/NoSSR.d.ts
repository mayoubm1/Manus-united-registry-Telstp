import { type ReactElement, type ReactNode } from 'react';
interface NoSSRProps {
    children: ReactNode;
}
declare const NoSSR: (props: NoSSRProps) => ReactElement;
export type { NoSSRProps };
export { NoSSR };
