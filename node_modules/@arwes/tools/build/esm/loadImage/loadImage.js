const loadImage = (url) => {
    return new Promise((resolve, reject) => {
        const img = new Image();
        img.onerror = img.onabort = () => reject();
        img.onload = () => resolve(img);
        img.src = url;
    });
};
export { loadImage };
