import { type ForwardedRef, type CSSProperties, type ReactElement } from 'react';
import { type CreateBackgroundMovingLinesSettings } from '@arwes/bgs';
interface MovingLinesProps extends CreateBackgroundMovingLinesSettings {
    elementRef?: ForwardedRef<HTMLCanvasElement>;
    id?: string;
    className?: string;
    style?: CSSProperties;
    positioned?: boolean;
}
declare const MovingLines: (props: MovingLinesProps) => ReactElement;
export { MovingLines };
