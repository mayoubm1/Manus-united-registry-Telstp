import React, { useRef, useEffect } from 'react';
import { cx } from '@arwes/tools';
import { memo, mergeRefs } from '@arwes/react-tools';
import { useAnimator } from '@arwes/react-animator';
import { createBackgroundGridLines } from '@arwes/bgs';
import { positionedStyle } from '../internal/styles.js';
const GridLines = memo((props) => {
    const { elementRef: elementRefExternal, id, className, style, positioned = true } = props;
    const animator = useAnimator();
    const elementRef = useRef(null);
    const settingsRef = useRef(props);
    settingsRef.current = props;
    useEffect(() => {
        const canvas = elementRef.current;
        if (!canvas) {
            return;
        }
        const background = createBackgroundGridLines({
            canvas,
            animator: animator?.node,
            settingsRef
        });
        return () => background.cancel();
    }, [animator]);
    return (React.createElement("canvas", { role: "presentation", ref: mergeRefs(elementRef, elementRefExternal), id: id, className: cx('arwes-bgs-gridlines', className), style: { ...(positioned ? positionedStyle : null), ...style } }));
});
export { GridLines };
